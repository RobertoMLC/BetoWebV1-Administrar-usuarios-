<%- include("./layouts/headDB") %>
    <div class="container">
        <h1>Aca deberia presentarce la tabla de la db para Admininstrar</h1>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">id</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Usuario</th>
                    <th scope="col">Rol</th>
                    <th scope="col">Contraseña</th>
                    <th scope="col"></th>
                </tr>
            </thead>
            <tbody>
                <% for(i=0; name.length> i; i++) { %>
                    <tr>
                        <th scope="row">
                            <%= i+1 %>
                        </th>
                        <td>
                            <%= name[i] %>
                        </td>
                        <td>
                            <%= user[i] %>
                        </td>
                        <td>
                            <div class="d-flex flex-row align-items-center mb-4">
                                <i class="fas fa-user fa-lg me-3 fa-fw"></i>
                                <div class="form-outline flex-fill mb-0">
                                    <select name="rol" id="rol" class="form-select" aria-label="Default select example">
                                        <option value="admin">Admin</option>
                                        <option value="data entry">Data Entry</option>
                                    </select>
                                    <label class="" for="form3Example1c">Registered with rol = <strong><%= rol[i] %></strong></label>
                                </div>
                            </div>


                        </td>
                        <td>
                            <%= pass[i] %>
                        </td>
                        <td>
                            <button class="btn btn-warning" id="<%= i %> ">Save Change</button>
                        </td>
                    </tr>

                    <% } %>
